[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    db: marks tests that require a live database connection (deselect with '-m "not db"')